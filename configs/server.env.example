# Keep this file readable only by root: chmod 600 /etc/reverse-tunnel/server.env
TUNNEL_TOKEN=replace-with-a-long-random-secret
TUNNEL_CONTROL=:7000
TUNNEL_PUBLIC_IP=0.0.0.0
TUNNEL_PORT_START=10000
TUNNEL_PORT_END=20000
